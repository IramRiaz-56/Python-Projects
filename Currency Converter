#Python Program
#Simple Currency Converter with fixed rates

rates = {
    'USD': 1,
    'PKR': 280,
    'INR': 83,
    'EUR': 0.91
}
amount = float(input("Enter amount in USD: "))
target = input("Convert to (PKR/INR/EUR): ").upper()

if target in rates:
    converted = amount * rates[target]
    print(f"{amount} USD = {converted} {target}")
else:
    print("Currency not supported.")
